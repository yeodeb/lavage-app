{% extends 'base1.html.twig' %}

{% block title %}Utilisateurs index{% endblock %}

{% block body %}
<!--main content start-->
    <section id="main-content">
      <section class="wrapper site-min-height">
        <div class="row mt">
          <div class="col-sm-12">
            <section class="panel">
              <header class="panel-heading">
                

    <h1>Voir en detail un utilisateur</h1>
              </header>
              <div class="panel-body">

    <table class="table">
        <tbody>
            <tr>
                <th>Numero</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Libelle de la prestation</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Prix unitaire</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>Date d'enregistrement</th>
                <td>{{ user.role }}</td>
            </tr>
        </tbody>
    </table>
    <hr>
    <a href="{{ path('user_index') }}" style="font-size:16px; color:red;">Retour a la liste</a>

    <!--a href="{{ path('user_edit', {'id': user.id}) }}">edit</a-->

    </div>
            </section>
          </div>
        </div>
        <!-- page end-->
      </section>
      <!-- /wrapper -->
    </section>
    <!-- /MAIN CONTENT -->
{% endblock %}
